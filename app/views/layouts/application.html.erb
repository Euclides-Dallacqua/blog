<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Blog" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>    
    <!-- Adicionar CSS do Bootstrap -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <style>
      body {
        font-family: 'Arial', sans-serif;
      }
      /* Estilo do cabeçalho */
      header {
        background-color: #282c34;
        color: #fff;
        padding: 15px 0;
        text-align: center;
        text-transform: uppercase;
        letter-spacing: 2px;
      }
      header h1 {
        background: rgba(255, 255, 255, 0.3);
        padding: 10px;
        border-radius: 10px;
      }
      /* Estilo dos botões */
      .btn {
        border-radius: 25px;
        padding: 10px 20px;
        transition: background-color 0.3s;
      }
      .btn:hover {
        background-color: #007bff;
        color: #fff;
      }
      .button {
        margin: 10px;
        display: inline-block;
      }
      /* Estilo do rodapé */
      footer {
        background-color: #282c34;
        color: #fff;
        text-align: center;
        padding: 20px;
        position: fixed;
        bottom: 0;
        width: 100%;
      }
      footer a {
        color: #fff;
        text-decoration: none;
        margin: 0 10px;
      }
      footer a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>Blog</h1>
      <div>
        <%= link_to "Home", root_path, class: "btn btn-light" %> |
        <% if user_signed_in? %>
          <%= current_user.email %>
          <%= link_to('Logout?', destroy_user_session_path, data: { turbo_confirm: 'Are you sure?', turbo_method: :delete }, class: "btn btn-danger") %>
          <%= link_to "Novo Artigo", new_article_path, class: "btn btn-primary" %>
        <% else %>
          <%= link_to "Cadastro", new_user_registration_path, class: "btn btn-warning" %>
          <%= link_to "Login", new_user_session_path, class: "btn btn-success" %>
        <% end %>
      </div>
    </header>

    <%= yield %>    

    <footer>
      <p>&copy; 2024 Blog | Todos os direitos reservados.</p>
      <a href="https://www.instagram.com" target="_blank">Instagram</a>
      <a href="https://www.facebook.com" target="_blank">Facebook</a>
    </footer>

    <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
  </body>
</html>



